export type PostInstagramRapid = {
  caption: {
    text: string;
    hashtags: [null];
  };
  code: string;
  thumbnail_url: string;
  user: {
    id: string;
    username: string;
    profile_pic_url: string;
  };
};
